/* global mount, it, describe, React, ReactDOM */

import React from 'react';
import { mount } from 'enzyme';
import ReactDOM from 'react-dom';
import App from './App.js';

describe('App', () => {
  it('matches snapshot', () => {

  });
});
